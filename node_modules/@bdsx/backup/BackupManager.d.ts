import { bedrockServer } from "bdsx";
import { IBackupSettings } from "./IBackupSettings";
export declare class BackupManager {
    private bds;
    private testOnly?;
    private tempName?;
    private worldName;
    private activePlayerCount;
    private runNextBackup;
    private lastBackup;
    private backupSettings;
    private bedrockServerPath;
    private resumeRetryCounter;
    constructor(bds: typeof bedrockServer, testOnly?: boolean | undefined, tempName?: string | undefined);
    init(settings: IBackupSettings): Promise<void>;
    backup(): Promise<void>;
    private registerHandlers;
    private runBackup;
    private displayStatus;
}
